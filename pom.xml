<?xml version="1.0" encoding="UTF-8"?><project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.arkticor</groupId>
  <artifactId>common</artifactId>
  <version>0.0.1-SNAPSHOT</version>
  <name>common</name>
  <packaging>pom</packaging>
  <description>Common library</description>
  <url/>
  <licenses>
    <license/>
  </licenses>
  <developers>
    <developer/>
  </developers>
  <scm>
    <connection/>
    <developerConnection/>
    <tag/>
    <url/>
  </scm>
  <properties>
    <java.version>25</java.version>
    <spring-boot.version>4.0.0</spring-boot.version>
  </properties>
  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring-boot.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>
  <build>
    <plugins>
      <plugin>
        <groupId>com.diffplug.spotless</groupId>
        <artifactId>spotless-maven-plugin</artifactId>
        <version>2.43.0</version>
        <configuration>
          <java>
            <!-- Use the standard Google Style (or Eclipse) -->
            <googleJavaFormat>
              <style>AOSP</style> <!-- AOSP style uses 4 spaces, closer to standard Java -->
            </googleJavaFormat>
            <!-- Or use the exact same Eclipse config you would use in revelc -->
            <!-- <eclipse><file>${project.basedir}/eclipse-fmt.xml</file></eclipse> -->
          </java>
          <pom>
            <sortPom></sortPom> <!-- Keeps your dependencies sorted automatically -->
          </pom>
        </configuration>
      </plugin>
    </plugins>
  </build>
  <modules>
    <module>common-core</module>
    <module>common-spring-boot-starter</module>
  </modules>
</project>
